#include "main.h"
/**
 * add - function to give a summation
 *
 * @m: calls the first variable
 *
 * @n: calls the second variable
 *
 * Return: add the two variables and give to the program
 */
int add(int m, int n)
{
	int results;

	results = m + n;

	return (results);
}
