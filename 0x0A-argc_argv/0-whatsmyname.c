#include "main.h"
#include <stdio.h>
/**
 * main - check code
 * @argc: the number of command line entries
 * @argv: the array reffered to by argc
 *
 * Return: always 0
 */ 
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
